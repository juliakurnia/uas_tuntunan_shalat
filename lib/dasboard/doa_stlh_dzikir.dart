// ignore_for_file: use_key_in_widget_constructors

import 'package:flutter/material.dart';

class DoaSetelahDzikirPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Doa Setelah Dzikir"),
      ),
      body: const Center(
        child: Text("Content for Doa Setelah Dzikir"),
      ),
    );
  }
}
